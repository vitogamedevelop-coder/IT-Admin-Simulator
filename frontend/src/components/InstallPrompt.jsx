import { useEffect, useState } from 'react';
import { Download } from 'lucide-react';
export default function InstallPrompt() { const [prompt, setPrompt] = useState(null); useEffect(() => { const handler = (event) => { event.preventDefault(); setPrompt(event); }; window.addEventListener('beforeinstallprompt', handler); return () => window.removeEventListener('beforeinstallprompt', handler); }, []); if (!prompt || window.matchMedia('(display-mode: standalone)').matches) return null; return <div className="cyber-card p-3"><p className="text-xs text-[#c9d1d9]">IT-Learn für schnelleren Offline-Zugriff als App installieren.</p><button onClick={() => { prompt.prompt(); setPrompt(null); }} className="cyber-btn-outline mt-2 text-xs py-2"><Download size={15} className="mr-1" />App installieren</button></div>; }
